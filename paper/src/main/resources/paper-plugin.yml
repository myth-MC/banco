name: banco
version: '${project.version}'
authors:
  - myth-MC
  - U8092
api-version: '1.20'
main: ovh.mythmc.banco.paper.BancoPaperPlugin
folia-supported: true

dependencies:
  server:
    Vault:
      load: BEFORE
      required: true
    PlaceholderAPI:
      load: AFTER
      required: false
    Towny:
      load: AFTER
      required: false

permissions:
  banco.user:
    default: true
  banco.admin:
    default: op